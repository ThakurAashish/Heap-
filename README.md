# Heap-
Heap is a complete binary tree stored in array form, where each parent satisfies a special ordering property: either parent ≥ children (max heap) or parent ≤ children (min heap). It supports efficient insertion, deletion, and priority queue operations.
